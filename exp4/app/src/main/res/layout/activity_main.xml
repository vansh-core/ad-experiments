<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="10dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Home Page - Grid Layout"
        android:textSize="22sp"
        android:textStyle="bold"
        android:gravity="center"
        android:padding="15dp"
        android:background="#3F51B5"
        android:textColor="#FFFFFF"
        android:layout_marginBottom="10dp"/>

    <!--
        GridView = shows items in 2D grid (rows and columns)
        numColumns = 2 means 2 items per row
        horizontalSpacing = gap between columns
        verticalSpacing = gap between rows
        stretchMode = columnWidth makes columns equal width
    -->
    <GridView
        android:id="@+id/gridView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:numColumns="2"
        android:horizontalSpacing="10dp"
        android:verticalSpacing="10dp"
        android:stretchMode="columnWidth"
        android:gravity="center"
        android:padding="5dp"/>

</LinearLayout>
